node_modules/
release-builds/
dist/
.env
certificates
oscar-macos
oscar-win.exe
.DS_Store

# Extensions copied in to be built into an installer (lib/extensions.js).
# They live in repositories of their own, and are never part of this one.
/extensions/

# Projects saved by a local OSCAR instance
projects/

# Private working notes, kept out of the public repo
docs/

# Local OSCAR settings (locked mode)
oscar-settings.json

# Agent worktrees and local Claude Code state -- never part of the project.
.claude/

# The exported page's runtime: build output (npm run build), inlined into every
# export by POST /export.
public/src/runtime.bundle.js
